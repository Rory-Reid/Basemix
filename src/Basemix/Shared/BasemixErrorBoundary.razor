@inherits ErrorBoundary

@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override Task OnErrorAsync(Exception exception)
    {
        File.AppendText("error.log").WriteLine(exception.ToString());
        return base.OnErrorAsync(exception);
    }
}